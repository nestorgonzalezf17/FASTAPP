### DOC

para poder usar la api ve a la siguiente direcion: http://[IP_ADDRESS]

### DOCUMENTACION

Para instalar las dependecias usa el siguiente comando: pip install -r requirements.txt

activar el entorno virtual usa el siguiente comando: .\Backend\Scripts\activate

para iniciar el servidor usa el siguiente comando: uvicorn main:app --reload

para consultar las apis locales ve a APITEST.http y usa RestClient para ejecutar las peticiones

para ejecutar el front end ve a la carpeta 


### CONSTRUCCION FRONT END

para iniciar el servidor ve a la carpeta front end y ejecuta el siguiente comando: npm run dev

para instalar las dependecias usa el siguiente comando: npm install

para ver la pagina ve a la siguiente direcion: http://localhost:5173/ usando ng serve -o dentro de la carpeta front-end